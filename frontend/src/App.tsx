import React from 'react';

export default function App() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center p-6">
      <div className="max-w-2xl w-full bg-[var(--shapes-bg-surface)] border border-[var(--shapes-border-subtle)] rounded-[var(--shapes-radius-lg)] p-8 shadow-lg">
        <h1 className="text-2xl font-bold text-[var(--shapes-violet-300)] mb-2">
          Shapes.inc — Delegated Negotiation
        </h1>
        <p className="text-[var(--shapes-text-secondary)]">
          Sub-thread negotiation feature initialization complete.
        </p>
      </div>
    </div>
  );
}
